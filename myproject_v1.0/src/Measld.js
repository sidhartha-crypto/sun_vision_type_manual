import React from 'react';

const Measld = ({ page }) => { 


     return (

       <>
       
       {page === 1 && (

        <div>
           mea sld 1 
        </div>

       )}
     {page === 2 && (

         <div>
              mea sld 2
        </div>

     )}
     
       </>
 
       );
};
export default Measld;