import React from 'react';

const Electricediting = ({ page }) => {

    return (
      <>
    {page === 1 && (

        <div>
          electric editing 1 
        </div>
    )}
    {page === 2 && (

         
        <div>
        electric editing 2
       </div>

     )}

     </>
 
      );

}; 
export default Electricediting;