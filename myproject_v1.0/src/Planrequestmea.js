import React from 'react';

const Planrequestmea = ({ page }) => { 

     return (
          <>
           {page === 1 && (
   
               <div>
                   plan reuest mea 1 
                </div>

           )}
           {page === 2 && (
                 
                 <div>
                    plan reuest mea 2 
                 </div>
           )}
          </>

     ); 
}; 
export default Planrequestmea;